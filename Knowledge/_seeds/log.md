# Knowledge log

Append-only. One row for every ingest, wiki search filed back into the Knowledge layer, lint pass,
or maintenance sweep — including searches that found nothing useful (`0 useful docs · no files
touched`). Never rewrite or prune rows; this log is how you reconstruct where a claim came from.

| Date | Operation | Summary | Files touched |
|---|---|---|---|
