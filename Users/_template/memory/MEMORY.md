# Memory index — [YOUR_NAME]

> Loaded at the start of every session, right after `config.md`. One line per memory file.
> Never put memory content here — only pointers. Full facts live in sibling `*.md` files.

<!--
Memory file format (one durable fact per file):

---
name: short-kebab-slug
description: one-line summary used to decide relevance
type: user | feedback | project | reference
date: YYYY-MM-DD
---

The fact. For feedback/corrections, include **Why:** and **How to apply:** lines.

Write triggers (immediately, not at session end):
- The user corrects the assistant           → type: feedback  (highest value — stops repeat errors)
- A decision is made with reasoning          → type: project
- A durable preference or working style      → type: user
- A stakeholder / channel / metric insight   → type: project
- A useful external pointer (doc, dashboard) → type: reference

Skip: task state (→ Tasks/), draft copy, raw data.
Verify memories against current files before citing — memories drift. On conflict, the
workspace file wins.
-->

## Index

- (empty — first entries are written by onboarding and by `/eod`)
